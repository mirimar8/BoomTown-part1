import { red } from '@material-ui/core/colors';

const styles = theme => ({
    card: {
        width: 500,
    },
    media: {
        height: 500,
    },
    avatar: {
        backgroundColor: red[500],
    },
});
export default styles;
