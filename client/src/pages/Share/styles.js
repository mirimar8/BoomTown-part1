const styles = theme => ({
    sharePage: {
        display: "flex",
        direction: "row",
        justifyContent: "space-evenly",
        alignContent: "center",
        paddingTop: theme.spacing(5),
    }
});

export default styles;
